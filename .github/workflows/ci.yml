name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  macos:
    name: macOS
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew update
          brew install ninja pkg-config qt opencv libraw little-cms2

      - name: Configure
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=15.0 \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt);$(brew --prefix opencv)"

      - name: Build
        run: cmake --build build

      - name: Test
        run: ctest --test-dir build --output-on-failure

      - name: Check macOS deployment compatibility
        run: tools/check_macos_compatibility.sh build/softloaf_trichrome_app.app
